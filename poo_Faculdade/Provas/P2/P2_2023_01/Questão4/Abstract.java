package poo_Faculdade.Provas.P2.P2_2023_01.Questão4;

public class Abstract {
    /*
    

    (a) private abstract int multiplica (double n1, int n2); - INVALIDA. Métodos abstratos não podem ser private, isso limita o acesso à implementação apenas dentro da classe abstrata.
    
    (b) public abstract void exibeResultado(); - Válida. Um método abstrato deve ser declarado como abstract e pode ter qualquer modificador de acesso.

    (c) public Calcula() {} - válida. Uma classe abstrata  pode ter construtores com implementação. Ela serve como modelo para outras classes, e a construção de objetos é feita nas classes derivadas.

    (d) private static final double VALOR=10; - Válida. Constantes podem ser declaradas em classes abstratas.

    (e) private double multiplica(double n1, double n2) {return - Válida. Métodos concretos (com implementação) podem ser declarados em classes abstratas.
    */
}
