package Inimigos;



public class FabricaInimigo {
    /*
 * private static Map<String, Inimigo> tiposInimigos = new HashMap<>();
    public static Inimigo criarInimigo(String nome) {
        String chave = nome;
        if (!tiposInimigos.containsKey(chave)) {
            tiposInimigos.put(nome, new Inimigo());
        } else {
            switch (tipo) {
                case "Esqueleto":
                    inimigo = new Esqueleto(m, nome);
                    break;
                // Adicione outros tipos de inimigos aqui
                default:
                    throw new IllegalArgumentException("Tipo de inimigo desconhecido: " + tipo);
            }
            tiposInimigos.put(tipo, inimigo);
        }
        return inimigo;
    }
 */
    
}
